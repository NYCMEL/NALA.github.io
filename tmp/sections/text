create an html with 10 sections and all hidden except the first one
with some content to be loaded from a url when it opens.

also create 10 buttons that when clicked, hide all open sections and open the clicked section
and ONLY if the content is empty, load it from the section url
